# Generated by nullwatch's CasaOS app watcher (internal/casaosapps) — do not
# edit by hand, it's regenerated on every scan and removed automatically
# when the app is uninstalled through CasaOS.
http:
  routers:
    {{.Slug}}:
      rule: "Host(`{{.Slug}}.{{.Domain}}`)"
      entryPoints:
        - websecure
      tls: {}
      service: {{.Slug}}
  services:
    {{.Slug}}:
      loadBalancer:
        servers:
          - url: "{{.TargetURL}}"
